import { BienImagesTafType } from './bien-images-taf-type';

describe('BienImagesTafType', () => {
  it('should create an instance', () => {
    expect(new BienImagesTafType()).toBeTruthy();
  });
});
