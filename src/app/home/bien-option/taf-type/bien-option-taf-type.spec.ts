import { BienOptionTafType } from './bien-option-taf-type';

describe('BienOptionTafType', () => {
  it('should create an instance', () => {
    expect(new BienOptionTafType()).toBeTruthy();
  });
});
