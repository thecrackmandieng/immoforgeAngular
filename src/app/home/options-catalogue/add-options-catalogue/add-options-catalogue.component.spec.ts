import { ComponentFixture, TestBed } from '@angular/core/testing';
import { AddOptionsCatalogueComponent } from './add-options-catalogue.component';
import { provideHttpClient } from '@angular/common/http';
import { provideHttpClientTesting } from '@angular/common/http/testing';
import { NgbActiveModal } from '@ng-bootstrap/ng-bootstrap';

describe('AddOptionsCatalogueComponent', () => {
  let component: AddOptionsCatalogueComponent;
  let fixture: ComponentFixture<AddOptionsCatalogueComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
       providers: [
        provideHttpClient(),
        provideHttpClientTesting(),
        NgbActiveModal
      ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(AddOptionsCatalogueComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
