import { ComponentFixture, TestBed } from '@angular/core/testing';
import { EditOptionsCatalogueComponent } from './edit-options-catalogue.component';
import { provideHttpClient } from '@angular/common/http';
import { provideHttpClientTesting } from '@angular/common/http/testing';
import { NgbActiveModal } from '@ng-bootstrap/ng-bootstrap';

describe('EditOptionsCatalogueComponent', () => {
  let component: EditOptionsCatalogueComponent;
  let fixture: ComponentFixture<EditOptionsCatalogueComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
       providers: [
        provideHttpClient(),
        provideHttpClientTesting(),
        NgbActiveModal
      ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(EditOptionsCatalogueComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
