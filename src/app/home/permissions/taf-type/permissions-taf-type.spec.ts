import { PermissionsTafType } from './permissions-taf-type';

describe('PermissionsTafType', () => {
  it('should create an instance', () => {
    expect(new PermissionsTafType()).toBeTruthy();
  });
});
