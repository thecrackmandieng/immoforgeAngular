import { RolePermissionTafType } from './role-permission-taf-type';

describe('RolePermissionTafType', () => {
  it('should create an instance', () => {
    expect(new RolePermissionTafType()).toBeTruthy();
  });
});
