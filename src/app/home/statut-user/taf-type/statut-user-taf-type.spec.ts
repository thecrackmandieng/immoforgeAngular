import { StatutUserTafType } from './statut-user-taf-type';

describe('StatutUserTafType', () => {
  it('should create an instance', () => {
    expect(new StatutUserTafType()).toBeTruthy();
  });
});
